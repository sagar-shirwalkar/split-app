{
  "name": "split-app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "node deploy.js",
    "deploy:local": "npm run build && npm run _copy-bundle && cd sn-sdk && now-sdk deploy --profile local",
    "deploy:pdi": "npm run build && npm run _copy-bundle && cd sn-sdk && now-sdk deploy --profile pdi",
    "_copy-bundle": "node scripts/copy-bundle.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
